// declare a function that has 2 parameters 
// multiply and determine # of monitors needed
// check if function works properly using console log

function monitorCount(rows, columns) {
  return rows * columns;
}

const numOfMonitors = monitorCount(5, 4);

console.log(numOfMonitors);
